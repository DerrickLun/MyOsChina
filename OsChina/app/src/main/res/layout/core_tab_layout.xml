<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical">

    <!--app:tabMode
        fixed: 固定tab的位置，当tab数量过多时，超出屏幕范围后。无法显示更多
        scrollable: tab数量超出屏幕范围，可以滚动tab，显示更多的tab-->
    <android.support.design.widget.TabLayout android:id="@+id/tab_layout"
        android:layout_width="match_parent" android:layout_height="45dp"
        android:background="?attr/colorPrimary" app:tabIndicatorColor="@android:color/white"
        app:tabSelectedTextColor="@android:color/white" app:tabTextColor="@color/white_transparent"
        app:tabGravity="fill" app:tabTextAppearance="?android:attr/textAppearanceSmall"
        app:tabMode="fixed"/>

    <ImageView android:id="@+id/iv_toolbar_shadow_id" android:layout_width="match_parent"
        android:layout_height="5dp" android:background="@drawable/d_shadow"
        android:contentDescription="@string/loading"/>
    <android.support.v4.view.ViewPager android:id="@+id/view_pager"
        android:layout_width="match_parent" android:layout_height="match_parent" />
</LinearLayout>
